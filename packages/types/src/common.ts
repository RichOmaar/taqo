/** A UUID string. */
export type UUID = string;

/** An ISO-8601 date-time string, e.g. "2026-07-09T12:00:00.000Z". */
export type ISODateString = string;

/** Arbitrary JSON object, used for configurable form payloads. */
export type JsonObject = Record<string, unknown>;
